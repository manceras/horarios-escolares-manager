"""The desktop shell: paths, backups, the local server and the window.

Nothing under ``app/api``, ``app/services`` or ``app/solver`` may import from
here. This package knows about the user's machine; the application does not.
"""

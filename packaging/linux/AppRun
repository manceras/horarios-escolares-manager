#!/bin/sh
# AppImage entry point. The bundle is relocatable, so everything is resolved
# relative to wherever the image happens to be mounted.
HERE=$(dirname "$(readlink -f "$0")")
exec "$HERE/usr/bin/Horarios" "$@"
